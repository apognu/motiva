name: Tag release

on:
  push:
    tags: ["*"]

permissions:
  contents: write
  packages: write
  id-token: write
  attestations: write

jobs:
  test:
    uses: ./.github/workflows/build.yml
    permissions:
      contents: read
    secrets: inherit

  release:
    uses: ./.github/workflows/release.yml
    permissions:
      contents: write
      id-token: write
      attestations: write
    needs: [test]
    secrets: inherit
    with:
      tag: ${{ github.ref_name }}

  docker:
    uses: ./.github/workflows/docker.yml
    permissions:
      contents: read
      packages: write
      id-token: write
      attestations: write
    needs: [test]
    secrets: inherit
    with:
      tag: ${{ github.ref_name }}
