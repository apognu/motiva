name: Main publishing

on:
  push:
    branches: [main]

permissions:
  contents: read
  packages: write

jobs:
  test:
    uses: ./.github/workflows/build.yml
    permissions:
      contents: read
    secrets: inherit

  docker:
    uses: ./.github/workflows/docker.yml
    needs: [test]
    permissions:
      contents: read
      packages: write
    secrets: inherit
    with:
      tag: latest
